# ─────────────────────────────────────────────────────────────────────────────
# RTV Field Image Classifier — Python Dependencies
# Install with: pip install -r requirements.txt
# Python 3.9+ required
# ─────────────────────────────────────────────────────────────────────────────

# Deep learning framework — we use PyTorch as specified by the assessment
torch>=2.0.0
torchvision>=0.15.0

# EfficientNet and other pretrained model architectures
timm>=0.9.0

# Image augmentation library — more powerful than torchvision transforms
# especially for field photography conditions (blur, rotation, colour shifts)
albumentations>=1.3.0

# Scientific computing and data handling
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.5.0
scikit-learn>=1.3.0

# Plotting — confusion matrix, training curves
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars during training
tqdm>=4.65.0

# API serving (also needed for Task 3)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6     # needed for file uploads in FastAPI
